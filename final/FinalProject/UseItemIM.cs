// I don't think I created the useItem.cs file, 
// so I created this so if someone else works  
// on the file the code won't conflict
/*
public class UseItem : Action
{
    // Class States


    // Constructor
    public UseItem(Player user, Entity target) : base(user, target) {}

    public UseItem(Player user, List<Entity> targets) : base(user, targets) {}


    // Methods
    public override void Effect()
    {
        // The effect of using an item is to get the item and then preform it's effect
        // Gets users list of Items and prints off of 
        List<Item> items = _user.Inventory.get();

        foreach(Item item in items)
        {
            Console.WriteLine(item.ImGetName);
        }
        
        // Gets desired item

        // Uses Desired item
        //I am tring to figure out how to use and delete the item
        items.ElementAt(userChoice)



    }
    
}
*/